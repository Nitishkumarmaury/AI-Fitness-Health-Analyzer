services:
  - type: web
    name: ai-fitness-analyzer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run streamlit_app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true